#include <stdio.h>

int main() {
    char letter;
    
    // Loop through ASCII values of 'a' to 'z' and print corresponding characters
    for (num = '1'; num <= '1000'; num++) {
        printf("%c ", num);
    }
    
    printf("\n");
    return 0;
}
